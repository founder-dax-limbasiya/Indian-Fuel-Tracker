packages:
  "recharts": "For the 30-day price trend line chart"
  "date-fns": "For formatting dates in the header and charts"
  "framer-motion": "For polished page transitions and staggered card animations"
  "clsx": "For conditional tailwind class merging"
  "tailwind-merge": "For merging tailwind classes safely"

images:
  - path: "artifacts/fuel-tracker/public/images/hero-bg.png"
    description: "Abstract fluid gradient background in subtle orange and blue tones, very clean and bright, premium corporate feel, plenty of negative space"
    aspect_ratio: "21:9"

notes:
  - Using generated hooks from @workspace/api-client-react
  - Injecting hardcoded initial data to satisfy the requirement for hardcoded data while still using the API hooks
